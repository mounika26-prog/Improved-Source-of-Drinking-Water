# ── Core analysis ─────────────────────────────────────────────────────────────
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
openpyxl>=3.0.0

# ── Streamlit dashboard ────────────────────────────────────────────────────────
streamlit>=1.35.0            # dashboard framework
plotly>=5.20.0               # interactive charts (used by app.py)

# ── Notebook environment (local Jupyter only) ─────────────────────────────────
jupyter>=1.0.0
notebook>=6.5.0
ipykernel>=6.0.0

# ── IBM Cloud / watsonx.ai integration ────────────────────────────────────────
# Optional — only needed for IBM Cloud deployment (see DEPLOYMENT.md).
# Pre-installed in watsonx.ai Studio notebook runtimes.
ibm-watsonx-ai>=1.0.0        # watsonx.ai Python SDK (APIClient, Credentials)
python-dotenv>=1.0.0          # load .env credentials file locally
